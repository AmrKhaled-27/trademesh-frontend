import { Typography } from '../../../components/Typography';

export const APIKey = () => {
  return (
    <div>
      <Typography variant="h2" className="mb-4">
        API Key
      </Typography>
    </div>
  );
};
